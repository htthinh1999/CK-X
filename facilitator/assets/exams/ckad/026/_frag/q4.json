{
  "id": "4",
  "namespace": "dispatch",
  "machineHostname": "ckad9999",
  "question": "The source of the dispatch board image is in `/home/candidate/exam/q4/app`. A container registry is running at `localhost:5000`.\n\n- Build the image `localhost:5000/dispatch-board:2.3` from that directory with the build arguments `DISPATCH_ZONE=riverside` and `BUILD_NO=417`, and push it to the registry. The pushed image must carry the labels `transit.dispatch/zone=riverside` and `transit.dispatch/build=417`, and its page must show `zone=riverside build=417`. You may edit the Dockerfile, but keep its base image.\n- Write only the value of the image's label `transit.dispatch/release`, as reported by `docker image inspect`, to `/home/candidate/exam/q4/release.txt`.\n- Create Deployment `dispatch-board` in namespace `dispatch` with `2` replicas and one container named `board` running image `localhost:5000/dispatch-board:2.3`. Both Pods must be Ready and serve the new page (`/usr/share/nginx/html/index.html`).",
  "concepts": [
    "docker build",
    "build arguments",
    "container registry",
    "image labels",
    "deployments"
  ],
  "verification": [
    {
      "id": "1",
      "description": "Registry holds dispatch-board:2.3 with labels zone=riverside, build=417, release=riverside-417",
      "verificationScriptFile": "q4_s1_validate_registry_image.sh",
      "expectedOutput": "0",
      "weightage": 2
    },
    {
      "id": "2",
      "description": "release.txt holds the value of label transit.dispatch/release",
      "verificationScriptFile": "q4_s2_validate_release_file.sh",
      "expectedOutput": "0",
      "weightage": 1
    },
    {
      "id": "3",
      "description": "Deployment dispatch-board: container board runs localhost:5000/dispatch-board:2.3 with 2/2 ready replicas",
      "verificationScriptFile": "q4_s3_validate_deployment.sh",
      "expectedOutput": "0",
      "weightage": 2
    },
    {
      "id": "4",
      "description": "Every ready dispatch-board Pod serves zone=riverside build=417",
      "verificationScriptFile": "q4_s4_validate_served_page.sh",
      "expectedOutput": "0",
      "weightage": 1
    }
  ]
}
