{
  "id": "10",
  "namespace": "permits",
  "machineHostname": "ckad9988",
  "question": "Workloads in namespace `permits` will pull images from a private registry.\n\n- Create a docker-registry Secret `permits-registry` for server `registry.permits.transit.local:5443`, username `permit-bot` and password `Gr33n$ignal-7`.\n- Make the namespace's `default` ServiceAccount use `permits-registry` as an image pull secret, in addition to the pull secret it already has.\n- Create a TLS Secret `permits-tls` from the certificate `/home/candidate/exam/q10/certs/gate.crt` and its private key. The key is one of the files in `/home/candidate/exam/q10/certs/keys/`.\n- The existing Pod `permit-check` must end up with both pull secrets of the `default` ServiceAccount in its spec. It must receive them from the ServiceAccount: do not list `imagePullSecrets` in the Pod yourself. Keep its name, its image `registry.k8s.io/pause:3.9` and its ServiceAccount; it must be Running.",
  "concepts": [
    "secrets",
    "service-accounts",
    "image-pull-secrets",
    "tls",
    "admission"
  ],
  "verification": [
    {
      "id": "1",
      "description": "permits-registry is a dockerconfigjson Secret with the exact server, username and password",
      "verificationScriptFile": "q10_s1_validate_registry_secret.sh",
      "expectedOutput": "0",
      "weightage": 1
    },
    {
      "id": "2",
      "description": "ServiceAccount default lists permits-registry and still lists legacy-pull",
      "verificationScriptFile": "q10_s2_validate_default_sa.sh",
      "expectedOutput": "0",
      "weightage": 1
    },
    {
      "id": "3",
      "description": "permits-tls is a kubernetes.io/tls Secret with gate.crt and its matching key",
      "verificationScriptFile": "q10_s3_validate_tls_secret.sh",
      "expectedOutput": "0",
      "weightage": 1
    },
    {
      "id": "4",
      "description": "Pod permit-check (pause:3.9, SA default) is Ready and has both legacy-pull and permits-registry in imagePullSecrets",
      "verificationScriptFile": "q10_s4_validate_pod_pull_secrets.sh",
      "expectedOutput": "0",
      "weightage": 2
    }
  ]
}
