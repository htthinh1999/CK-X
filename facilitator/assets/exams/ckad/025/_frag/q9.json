{
  "id": "9",
  "namespace": "antenna",
  "machineHostname": "ckad9988",
  "question": "In namespace `antenna`, the Pods of Deployment `dish-receiver` are Running, but the NodePort Service `dish-receiver` has no endpoints.\n\nFix the **Service** only (do not modify the Deployment) so that:\n- its port `8080` forwards to the Deployment's container port by **name** (use the port name the container declares, not a number);\n- it is exposed on nodePort `30725`;\n- it keeps type `NodePort`, port `8080` and its selector `app=dish-receiver`;\n- both `dish-receiver` Pods are registered as endpoints of the Service.",
  "concepts": ["services", "nodeport", "named-ports", "endpoints", "troubleshooting"],
  "verification": [
    {"id": "1", "description": "Service port 8080 targets the named container port rx-http; Deployment and selector unchanged", "verificationScriptFile": "q9_s1_validate_targetport.sh", "expectedOutput": "0", "weightage": 1},
    {"id": "2", "description": "Service is NodePort with port 8080 on nodePort 30725", "verificationScriptFile": "q9_s2_validate_nodeport.sh", "expectedOutput": "0", "weightage": 1},
    {"id": "3", "description": "Service has 2 ready endpoints on container port 80", "verificationScriptFile": "q9_s3_validate_endpoints.sh", "expectedOutput": "0", "weightage": 2}
  ]
}
