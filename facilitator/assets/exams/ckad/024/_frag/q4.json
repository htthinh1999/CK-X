{
  "id": "4",
  "namespace": "berth",
  "machineHostname": "ckad9999",
  "question": "A local Helm chart is available at `/home/candidate/exam/q4/dockyard`. Two releases of it are installed in namespace `berth`: `tugboat` and `pilot-legacy`.\n\n1. Upgrade release `tugboat` with the same chart so that it runs `2` replicas (chart value `replicaCount`) and its pods get the annotation `crew: night-shift` (chart value `podAnnotations.crew`). The values already set on the release (such as its existing pod annotation) must be preserved.\n2. Uninstall the release `pilot-legacy`.\n3. Install a new release named `harbormaster` from the same chart into namespace `berth`, with `service.type` set to `NodePort`.",
  "concepts": ["helm", "helm upgrade", "helm uninstall", "helm install", "values"],
  "verification": [
    { "id": "1", "description": "tugboat upgraded (deployed, revision >= 2) with replicaCount=2 and 2 ready pods", "verificationScriptFile": "q4_s1_validate_tugboat_replicas.sh", "expectedOutput": "0", "weightage": 1 },
    { "id": "2", "description": "tugboat pods annotated crew=night-shift and the existing owner=berth-ops value is kept", "verificationScriptFile": "q4_s2_validate_tugboat_annotations.sh", "expectedOutput": "0", "weightage": 1 },
    { "id": "3", "description": "Release pilot-legacy and its resources are removed", "verificationScriptFile": "q4_s3_validate_legacy_removed.sh", "expectedOutput": "0", "weightage": 1 },
    { "id": "4", "description": "Release harbormaster (chart dockyard) is deployed in berth with a NodePort Service", "verificationScriptFile": "q4_s4_validate_harbormaster.sh", "expectedOutput": "0", "weightage": 1 }
  ]
}
