{
  "id": "11",
  "namespace": "winch",
  "machineHostname": "ckad9988",
  "question": "In namespace `winch` the Deployment `hoist-controller` runs a container `hoist` that appends a line every 3 seconds to `/var/log/hoist/cycles.log` on an `emptyDir` volume named `hoist-logs`, so nothing shows up in `kubectl logs`. Its manifest is at `/home/candidate/exam/q11/hoist-controller.yaml`.\n\nAdd a logging sidecar to the Deployment as a second regular container (under `containers`, not `initContainers`):\n- name `log-tail`, image `busybox:1.36`\n- mounts the existing `hoist-logs` volume at `/var/log/hoist`\n- command `[\"sh\", \"-c\", \"tail -n +1 -F /var/log/hoist/cycles.log\"]`\n\nLeave the `hoist` container unchanged. After the rollout, `kubectl logs` for the `log-tail` container of the running Pod must show the `hoist cycle complete` lines.",
  "concepts": [
    "multi-container",
    "sidecar",
    "logging",
    "volumes"
  ],
  "verification": [
    {
      "id": "1",
      "description": "Deployment hoist-controller has containers hoist and log-tail (busybox:1.36)",
      "verificationScriptFile": "q11_s1_validate_containers.sh",
      "expectedOutput": "0",
      "weightage": 1
    },
    {
      "id": "2",
      "description": "log-tail mounts the hoist-logs emptyDir at /var/log/hoist, shared with hoist",
      "verificationScriptFile": "q11_s2_validate_shared_mount.sh",
      "expectedOutput": "0",
      "weightage": 1
    },
    {
      "id": "3",
      "description": "kubectl logs -c log-tail of the running pod shows the hoist cycle lines",
      "verificationScriptFile": "q11_s3_validate_sidecar_logs.sh",
      "expectedOutput": "0",
      "weightage": 2
    }
  ]
}
