{
  "id": "10",
  "namespace": "yard",
  "machineHostname": "ckad9988",
  "question": "In namespace `yard` the Deployment `forklift` is already running. The manifest `/home/candidate/exam/q10/forklift-hpa.yaml` defines a HorizontalPodAutoscaler for it, but it uses `autoscaling/v2beta2`, which the cluster no longer serves, so `kubectl apply` fails.\n\nEdit the file in place so that it:\n- uses `apiVersion: autoscaling/v2`\n- keeps the name `forklift-hpa` and the target Deployment `forklift`\n- sets `minReplicas: 2`, `maxReplicas: 5` and a CPU target of type `Utilization` with `averageUtilization: 60`\n\nApply the file. Once applied, the HPA scales `forklift` up to its new minimum on its own; make sure `forklift` ends up with at least 2 ready replicas.",
  "concepts": [
    "hpa",
    "autoscaling",
    "api-deprecation"
  ],
  "verification": [
    {
      "id": "1",
      "description": "forklift-hpa.yaml uses autoscaling/v2 with min 2, max 5, cpu Utilization 60",
      "verificationScriptFile": "q10_s1_validate_file.sh",
      "expectedOutput": "0",
      "weightage": 1
    },
    {
      "id": "2",
      "description": "Live HPA forklift-hpa targets Deployment forklift with min 2, max 5, cpu Utilization 60",
      "verificationScriptFile": "q10_s2_validate_hpa.sh",
      "expectedOutput": "0",
      "weightage": 2
    },
    {
      "id": "3",
      "description": "Deployment forklift has at least 2 ready replicas",
      "verificationScriptFile": "q10_s3_validate_scaled.sh",
      "expectedOutput": "0",
      "weightage": 1
    }
  ]
}
