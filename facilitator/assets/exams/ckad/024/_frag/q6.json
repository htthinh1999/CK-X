{
  "id": "6",
  "namespace": "gatehouse",
  "machineHostname": "ckad9999",
  "question": "In namespace `gatehouse`, create a Deployment named `boom-gate` with `3` replicas. Its only container, named `controller`, uses image `busybox:1.36` and runs:\n\n`sh -c 'sleep 15; touch /tmp/gate-open; while true; do sleep 3600; done'`\n\nThe gate needs about 15 seconds to warm up and must not receive traffic before `/tmp/gate-open` exists. Add a **readiness probe** to the container that executes `cat /tmp/gate-open`, with `initialDelaySeconds: 5` and `periodSeconds: 5`.\n\nMake sure all 3 replicas become ready.",
  "concepts": ["probes", "readiness probe", "deployments", "exec"],
  "verification": [
    { "id": "1", "description": "boom-gate has 3 replicas; container controller (busybox:1.36) runs the given command", "verificationScriptFile": "q6_s1_validate_deployment.sh", "expectedOutput": "0", "weightage": 1 },
    { "id": "2", "description": "exec readinessProbe on /tmp/gate-open with initialDelaySeconds=5 and periodSeconds=5", "verificationScriptFile": "q6_s2_validate_probe.sh", "expectedOutput": "0", "weightage": 2 },
    { "id": "3", "description": "All 3 boom-gate replicas are updated, ready and available", "verificationScriptFile": "q6_s3_validate_ready.sh", "expectedOutput": "0", "weightage": 1 }
  ]
}
