{
  "id": "8",
  "namespace": "customs",
  "machineHostname": "ckad9988",
  "question": "A customs clearance app in namespace `customs` needs broker credentials.\n\n1. Create a generic Secret named `broker-creds` with two keys: `api-token` = `tk-7731-harbor` and `broker-id` = `HL-0042`.\n2. Create a Pod named `declarations` with a single container named `clerk` (image `nginx:1.25`) that:\n   - mounts the **whole** Secret `broker-creds` (every key, no `items` list) as a volume at `/etc/customs/creds`, with the volume mount set to `readOnly: true`\n   - exposes the Secret key `broker-id` as the environment variable `CUSTOMS_BROKER_ID` using `secretKeyRef` (not a literal value)\n\nThe Pod must be `Running`.",
  "concepts": [
    "secrets",
    "volumes",
    "env",
    "pods"
  ],
  "verification": [
    {
      "id": "1",
      "description": "Secret broker-creds (Opaque) has api-token=tk-7731-harbor and broker-id=HL-0042",
      "verificationScriptFile": "q8_s1_validate_secret.sh",
      "expectedOutput": "0",
      "weightage": 1
    },
    {
      "id": "2",
      "description": "Whole secret mounted at /etc/customs/creds in container clerk (both key files readable)",
      "verificationScriptFile": "q8_s2_validate_mount.sh",
      "expectedOutput": "0",
      "weightage": 1
    },
    {
      "id": "3",
      "description": "The secret volume mount is readOnly: true",
      "verificationScriptFile": "q8_s3_validate_readonly.sh",
      "expectedOutput": "0",
      "weightage": 1
    },
    {
      "id": "4",
      "description": "CUSTOMS_BROKER_ID comes from secretKeyRef broker-creds/broker-id and printenv shows HL-0042",
      "verificationScriptFile": "q8_s4_validate_env.sh",
      "expectedOutput": "0",
      "weightage": 1
    }
  ]
}
